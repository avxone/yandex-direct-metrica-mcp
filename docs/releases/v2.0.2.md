# v2.0.2

Date: 2026-02-05

## Changes

- Fixed `direct.list_clients`: the Direct `clients` endpoint does not accept `SelectionCriteria`, so the tool no longer sends it by default (prevents `YandexDirectClientError` code `8000`).
- Relaxed `direct_client_login` tool schema: removed hard `enum` restriction and kept it as a hint-only field for agency setups.

## Notes

- Public vs Pro contract remains unchanged:
  - Public images are safe-by-default and read-only.
  - Pro features remain gated by build edition and runtime flags.

