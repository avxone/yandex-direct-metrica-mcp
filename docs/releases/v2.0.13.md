# v2.0.13

## Highlights

- Added the bounded `search_serp` MCP tool for Yandex Search API Web Search.
- Restored the missing runtime implementation from the published GEO-10 PR commit and verified it against the real provider.
- Hardened live validation for Wordstat monthly dynamics by retrying a buffered closed month when the freshest month is not provider-ready yet.
- Finalized the Symphony `feature -> PR -> release` issue chain docs, capability contracts, and follow-up issue automation.

## Included

- `search_serp` tool contract and runtime implementation:
  - `src/mcp_yandex_ad/search_client.py`
  - `src/mcp_yandex_ad/server.py`
  - `src/mcp_yandex_ad/tools.py`
  - `src/mcp_yandex_ad/tool_contracts.py`
- Search SERP tests and public snapshots:
  - `tests/test_search_serp.py`
  - `tests/snapshots/public_tool_contracts_v1.json`
  - `tests/snapshots/public_tools_v1.json`
- Client handoff for `Marketing2025`:
  - `docs/marketing2025-search-serp-handoff-2026-06-28.md`
- Symphony workflow/process updates:
  - `docs/automation/*`
  - `scripts/linear_issue.py`
  - `tests/test_linear_issue_followups.py`

## Validation

- `python -m compileall -q src/mcp_yandex_ad scripts/check_wordstat_access.py scripts/check_search_serp_access.py`
- `pytest -q`
- `python scripts/agent_lint.py`
- `source <state-root>/yandex.ad/.env`
- `.venv/bin/python scripts/live_validation.py --suite direct,metrica,wordstat,search`
- `python scripts/release_guard.py --version 2.0.13 --require-release-notes`

## Live provider notes

- Direct access passed against `api.direct.yandex.com`.
- Metrica management/stats access passed for counters `40029465`, `91450749`, and `1446613`.
- Wordstat `dynamics` for the freshest closed month still returned provider `HTTP 400` on July 1, 2026, so the validation harness now retries one buffered month earlier and records the actual month used.
- Search API Web Search passed with normalized ads and organic results for the bounded smoke query.
