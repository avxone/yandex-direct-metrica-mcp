# Session: Issue boundary rules and GEO-7 reframe

Date: 2026-06-25

## Completed

- Reframed `GEO-7` from cross-repo migration scope to MCP-only server scope plus client handoff.
- Rewrote the English and Russian issue drafts for `search_serp` around that boundary.
- Added general issue-writing rules to prevent producer/consumer scope mixing.
- Updated the generic feature template and the Marketing2025 workflow template with explicit ownership-boundary fields.
- Added the `release-required` feature pattern so published images can be part of a feature issue when the client cannot consume the change otherwise.

## To Do

- Update the live Linear issue `GEO-7` title and description to match the reframed server-only scope.
- Add the `release-required` label to the live Linear issue `GEO-7`.
- Re-run Symphony on `GEO-7` after the updated scope is visible in Linear.
