# Session - 2026-05-08 - Marketing release handoff

## Completed

- Added a focused post-release handoff for `Marketing2025` covering:
  - `v2.0.10` release status
  - public / PRO publish status
  - MCP-side fixes now shipped
  - local `:dev` image alignment for `Marketing2025`
  - the remaining joint replay step
- Added the Russian counterpart in `docs/ru/`.
- Updated `CHANGELOG.md` with the new handoff note.

## To Do

- Jointly run the intended `Marketing2025` replay / pipeline validation against the released MCP image path.
- Receive the promised Path A input PR from `Marketing2025` into `yandex.ad/docs/`.
- Optionally expand PRO BI QA coverage to exercise more `dashboard.dataset.*` tools directly.
